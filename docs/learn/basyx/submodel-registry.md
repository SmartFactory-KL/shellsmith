# Submodel Registry

The **Submodel Registry** stores **Submodel descriptors**. Descriptors do not contain the full Submodel payload; they point clients to the endpoint where the Submodel is hosted.

Use it when Submodels are distributed across multiple servers and need a central lookup service.
